console.log(`
🎉 Hackpack installed!

👉 To enable tab-completion, run one of the following:

  # Linux / macOS (bash/zsh)
  hp completion >> ~/.bashrc && source ~/.bashrc

  # Zsh
  hp completion >> ~/.zshrc && source ~/.zshrc

  # PowerShell (Windows)
  hp completion >> $PROFILE
  . $PROFILE

⚡ Now enjoy auto-completion with "hp"!
`);
